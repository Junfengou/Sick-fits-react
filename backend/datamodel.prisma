# [@unique] is directive

type User {
	id: ID! @id
	name: String!
	email: String!
}

# [String!] means it's required

type Item {
	id: ID! @id
	title: String!
	description: String!
	image: String
	largeImage: String
	price: Int!
	createdAt: DateTime
	updateAt: DateTime! @createdAt
	# user: User!
}
